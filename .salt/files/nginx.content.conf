{% set cfg = salt['mc_utils.json_load'](data).cfg%}
{% set data = cfg.data %}
location /static/ {
    alias {{data.static}}/;
}
location /media/ {
    alias {{data.media}}/;
}
location / {
    include proxy_params;
    proxy_pass http://{{cfg.name}}app;
}
