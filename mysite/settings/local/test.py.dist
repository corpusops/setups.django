"""
Override final test environment settings
"""
import os

from ...test import *

INSTALLED_APPS += (
    'mysite.apps.apptest',  # WARNING - just for testing, REMOVE ME!
)

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.sqlite3',
        'NAME': os.path.join(BASE_DIR, 'db.sqlite3'),
    }
}
